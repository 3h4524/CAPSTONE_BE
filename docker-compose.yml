services:
  redis:
    image: redis:7-alpine
    container_name: apcs-redis
    ports:
      - "6379:6379"
    volumes:
      - apcs_redis_data:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  apcs_redis_data:
